{
  "name": "mount-autumn-ui",
  "repository": {
    "type": "git",
    "url": "https://github.com/surrsurus/mount-autumn-ui"
  },
  "downloads": 835,
  "stargazers_count": 1,
  "releases": {
    "latest": "2.2.0"
  },
  "versions": {
    "2.2.0": {
      "name": "mount-autumn-ui",
      "theme": "ui",
      "version": "2.2.0",
      "description": "Autumnal theme for Atom based on One Dark and Pumpkin Syntax",
      "keywords": [
        "dark",
        "autumn",
        "ui"
      ],
      "license": "GPL 3",
      "repository": "https://github.com/surrsurus/mount-autumn-ui",
      "main": "lib/main",
      "engines": {
        "atom": ">0.40.0"
      },
      "devDependencies": {
        "coffeelint": "^1.9.7"
      },
      "configSchema": {
        "fontSize": {
          "title": "Font Size",
          "description": "Change the UI font size. Needs to be between 8 and 20. In Auto mode, the Font Size will automatically change based on the window size.",
          "type": [
            "integer",
            "string"
          ],
          "minimum": 8,
          "maximum": 20,
          "default": "Auto"
        },
        "layoutMode": {
          "title": "Layout Mode",
          "description": "In Auto mode, the UI will automatically adapt based on the window size.",
          "type": "string",
          "default": "Auto",
          "enum": [
            "Compact",
            "Auto",
            "Spacious"
          ]
        },
        "tabSizing": {
          "title": "Tab Sizing",
          "description": "In minimum width mode the tabs will be as small as possible but in even width mode, all tabs will be the same size. In auto mode the tabs switch based on the window size.",
          "type": "string",
          "default": "Auto",
          "enum": [
            "Even",
            "Auto",
            "Minimum"
          ]
        }
      },
      "dist": {
        "tarball": "https://www.atom.io/api/packages/mount-autumn-ui/versions/2.2.0/tarball"
      }
    }
  },
  "readme": "# mount-autumn-ui\n\nAn Atom theme based on One Dark and Pumpkin Syntax\n",
  "metadata": {
    "name": "mount-autumn-ui",
    "theme": "ui",
    "version": "2.2.0",
    "description": "Autumnal theme for Atom based on One Dark and Pumpkin Syntax",
    "keywords": [
      "dark",
      "autumn",
      "ui"
    ],
    "license": "GPL 3",
    "repository": "https://github.com/surrsurus/mount-autumn-ui",
    "main": "lib/main",
    "engines": {
      "atom": ">0.40.0"
    },
    "devDependencies": {
      "coffeelint": "^1.9.7"
    },
    "configSchema": {
      "fontSize": {
        "title": "Font Size",
        "description": "Change the UI font size. Needs to be between 8 and 20. In Auto mode, the Font Size will automatically change based on the window size.",
        "type": [
          "integer",
          "string"
        ],
        "minimum": 8,
        "maximum": 20,
        "default": "Auto"
      },
      "layoutMode": {
        "title": "Layout Mode",
        "description": "In Auto mode, the UI will automatically adapt based on the window size.",
        "type": "string",
        "default": "Auto",
        "enum": [
          "Compact",
          "Auto",
          "Spacious"
        ]
      },
      "tabSizing": {
        "title": "Tab Sizing",
        "description": "In minimum width mode the tabs will be as small as possible but in even width mode, all tabs will be the same size. In auto mode the tabs switch based on the window size.",
        "type": "string",
        "default": "Auto",
        "enum": [
          "Even",
          "Auto",
          "Minimum"
        ]
      }
    }
  }
}